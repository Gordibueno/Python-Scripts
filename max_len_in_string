# Longest word in string

line = "What if we try a super-long word such as otorhinolaryngology"

newList = line.split(" ")

longest = 0
for i in newList:
    if len(i) > longest:
        longest = len(i)
    else:
        pass
print longest